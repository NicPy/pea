export const DEFAULT_LOCALE = 'en-US'
export const DEFAULT_TIME_FORMATTING_OPTIONS = {
    hour: 'numeric',
    minute: '2-digit',
    second: '2-digit',
    hour12: false, // Ensures 24-hour format
}
export const DEFAULT_DATE_FORMATTING_OPTIONS = {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric',
}
